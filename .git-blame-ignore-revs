# Commits to skip when running git blame, so a whole-file reformat does not
# take the blame for every line in it.
# Use with: git blame --ignore-revs-file .git-blame-ignore-revs
# or once per clone: git config blame.ignoreRevsFile .git-blame-ignore-revs

# style: format the legacy js and jsx files with prettier
f119bb96d6363c42e025daf70cdddc91e9d625c0
